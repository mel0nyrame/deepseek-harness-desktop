# `@dsh-desktop/native`

Native capability providers for DSH Desktop. Directory selection, path
opening, and similar macOS actions are exposed as explicit Cordis capability
providers with reverse requests through the DSH child boundary — never as
ad-hoc renderer calls or model-loop logic.

Implementation lands with decoupling 6/10.
