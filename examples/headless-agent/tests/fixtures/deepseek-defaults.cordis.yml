- id: base
  name: '@deepseek-ai/cordis-plugin-include'
  config:
    path: ../../cordis.yml
    patches:
      - id: llm-deepseek
        config:
          baseURL: !!js process.env.DSH_SNAPSHOT_BASE_URL
<<<<<<< HEAD
          thinking: disabled
          # Outlast the comments-only prefix (12 × 100ms) so only transport
          # comments — never a data chunk — can rearm the watchdog.
          streamIdleTimeoutMs: 1000
=======
          thinking: enabled
          reasoningEffort: low
          streamIdleTimeoutMs: 150
>>>>>>> upstream/master
      - id: agent-spine
        config:
          agents:
            - id: main
              provider: deepseek-official
              model: deepseek-v4-flash
              cwd: !!js process.cwd()
          workspaceContext: false
          persona: 'Keyless DeepSeek adapter defaults snapshot.'
      - id: persistence
        config:
          root: './.sessions'
